services:
  - type: web
    name: nexgen-markets
    runtime: python
    buildCommand: echo "No build step needed"
    startCommand: python server.py
    envVars:
      - key: PORT
        generateValue: false
